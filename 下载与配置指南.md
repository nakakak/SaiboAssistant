# 赛搏小小助手（SaiboAssistant）v0.2.14 — 下载、配置与使用指南

本文仅说明 **v0.2.14** 这一版 Release 中 **`SaiboAssistant-*` 单文件** 的下载与配置流程。  
请下载对应系统的单文件后直接运行，**无需解压**；首次运行会在程序旁自动生成 `config.yaml` 并进入图形向导。

> ## ⚠️ 设备配对方式（必读）
>
> | 项目 | v0.2.14 当前状态 |
> |------|------------------|
> | **设备 MAC 地址** | ✅ **唯一可用**：须在向导/连接设置中选 **「设备 MAC」** 并填写 |
> | **6 位验证码 / 连接码** | ❌ **尚不能使用**（界面可能仍显示该选项，**请勿选择、请勿填写**） |
>
> 使用 6 位验证码自动配对、向云端解析 MAC 等功能属于 **后期规划**，将在后续版本实现并更新本文档。  
> **当前若误选连接码，将无法连上商家云或行为不可预期，请务必使用 MAC。**

---

## 一、下载 v0.2.14

### 1.1 Release 页面

打开 **v0.2.14** 版本页：

**https://github.com/nakakak/SaiboAssistant/releases/tag/v0.2.14**

在 **Assets** 中下载 **与你系统对应的一个 `SaiboAssistant-*` 文件**（不要点 *Source code*）。

| 你的电脑 | 下载文件名 |
|----------|------------|
| Windows 64 位 | `SaiboAssistant-Windows-x64.exe` |
| Mac Apple Silicon（M 系列） | `SaiboAssistant-macOS-arm64` |
| Mac Intel | `SaiboAssistant-macOS-x64` |
| Linux x86_64 | `SaiboAssistant-Linux-x64` |
| Linux arm64 | `SaiboAssistant-Linux-arm64` |

### 1.2 固定直链（v0.2.14）

```text
https://github.com/nakakak/SaiboAssistant/releases/download/v0.2.14/SaiboAssistant-Windows-x64.exe
https://github.com/nakakak/SaiboAssistant/releases/download/v0.2.14/SaiboAssistant-macOS-arm64
https://github.com/nakakak/SaiboAssistant/releases/download/v0.2.14/SaiboAssistant-macOS-x64
https://github.com/nakakak/SaiboAssistant/releases/download/v0.2.14/SaiboAssistant-Linux-x64
https://github.com/nakakak/SaiboAssistant/releases/download/v0.2.14/SaiboAssistant-Linux-arm64
```

### 1.3 命令行下载示例

**macOS Apple Silicon：**

```bash
curl -L -o SaiboAssistant-macOS-arm64 \
  https://github.com/nakakak/SaiboAssistant/releases/download/v0.2.14/SaiboAssistant-macOS-arm64
chmod +x SaiboAssistant-macOS-arm64
```

**Windows（PowerShell）：**

```powershell
Invoke-WebRequest -Uri "https://github.com/nakakak/SaiboAssistant/releases/download/v0.2.14/SaiboAssistant-Windows-x64.exe" -OutFile "SaiboAssistant-Windows-x64.exe"
```

**Linux x64：**

```bash
curl -L -o SaiboAssistant-Linux-x64 \
  https://github.com/nakakak/SaiboAssistant/releases/download/v0.2.14/SaiboAssistant-Linux-x64
chmod +x SaiboAssistant-Linux-x64
```

可选：在同一 Release 页下载 `checksums-saibo-sha256.txt`，用 `shasum -a 256` 校验下载文件。

---

## 配置截图（图文对照）

截图文件放在仓库 [`docs/images/`](./docs/images/) 目录。将你自己的 PNG/JPG 按 [docs/images/README.md](./docs/images/README.md) 命名放入后，下方图片会自动显示。

> 若图片暂未上传，会显示裂图；按 README 放入文件并 push 到 GitHub 即可。

### 下载与安装（Mac 示例，可选）

<!-- 有图时取消下行注释
![GitHub Release 下载页](./docs/images/01-download-release.png)
![Mac 安全提示：右键打开或 xattr](./docs/images/02-mac-gatekeeper.png)
-->

---

## 二、下载后如何启动

1. 将下载的 **单个文件** 放到任意目录（如 `~/SaiboAssistant/` 或桌面）。
2. **Mac / Linux**（若尚未可执行）：
   ```bash
   chmod +x SaiboAssistant-macOS-arm64   # 文件名按实际修改
   ./SaiboAssistant-macOS-arm64
   ```
3. **Windows**：双击 `SaiboAssistant-Windows-x64.exe`。

**首次运行：**

- 在 **可执行文件同目录** 自动生成 `config.yaml`（目录不可写时，写入系统用户配置目录下的 `SaiboAssistant/config.yaml`）。
- 弹出 **「赛搏小小助手 — 首次配置」** 向导（3 步：功能选择 → 连接信息 → 权限与同意）。

---

## 三、首次配置向导（界面说明）

> **v0.2.14 配对说明（重要）**  
> - **当前只能**用 **设备 MAC 地址** 连接商家云。  
> - **6 位验证码（连接码）还不能用**：程序里若出现「连接码（6 位数字）」等字样，是预留界面，**v0.2.14 未实现该能力**；选它或填 6 位数字 **不能完成配对**。  
> - 请一律改选 **「设备 MAC」**，填写与管理台一致的 MAC（如 `98:88:e0:14:79:a0`）。  
> - 验证码配对、管理台生成连接码后一键填入等功能，**留待后续版本**再开放。

### 第 1 步：欢迎与功能选择

- **OpenClaw：云端任务发到本电脑** — 设备经商家云把任务派到本机 OpenClaw。
- **听写：语音转文字传到本电脑** — 听写内容经云端转发到本机（可注入输入框）。

**至少勾选一项**，点 **「下一步」**。

![首次向导 — 功能选择](./docs/images/03-first-run-welcome.png)

### 第 2 步：商家云与设备配对（必填）

#### （1）商家云地址（必填，须手动填写）

**v0.2.14** 首次向导**不会预填**地址，请按你的商家云实际地址填写。须包含路径 **`/bridge/connector`**，**不要**手写 `?token=` 等 query。

**格式：** `ws://` 或 `wss://` + 域名或 IP + 端口（若需要）+ `/bridge/connector`

##### 环境对照表（与赛搏 AI 管理后台一致）

| 环境 | 商家云登录地址 | **SaiboAssistant 填写的 server_url** |
|------|----------------|--------------------------------------|
| **公网云（推荐线上）** | https://xiaozhi.cyberai.top | **`wss://xiaozhi.cyberai.top/bridge/connector`** |
| 局域网测试（如 192.168.50.52） | http://192.168.50.52:8084 | `ws://192.168.50.52:8084/bridge/connector` |
| 本机开发 | http://127.0.0.1:8084 | `ws://127.0.0.1:8084/bridge/connector` |

说明：上表「商家云登录地址」为 **Web 入口**（用户/管理员均从此登录）；登录后在菜单进入 **设备管理** 等页面即可，**不必**在地址后加 `/admin`。

说明：

- 管理台 **Web 配置** 里的「WebSocket 地址」（如 `wss://xiaozhi.cyberai.top/ws`）是给 **设备语音传输** 用的，**不是** 赛搏小小助手的地址。
- 助手必须填带 **`/bridge/connector`** 的地址（上表第三列）。
- 公网云若走 HTTPS 反代，一般用 **`wss://`** 且**不写**管理台里的内部端口（8085）；若直连端口再试 `wss://xiaozhi.cyberai.top:8085/bridge/connector`。

##### 切换商家云时（局域网 → 公网云）

1. 托盘 → **连接设置**
2. **商家云 WebSocket** 改为：`wss://xiaozhi.cyberai.top/bridge/connector`
3. 确认 **设备 MAC** 已在**目标云**管理台注册（换云后须在该云重新添加/绑定设备）
4. 点 **保存并重连**
5. 公网云服务器数据库里须有 **`bridge_enabled: true`** 并已重启服务（与局域网改库同理）

##### 连上前自检（公网云）

```bash
# 不要用 /health 判断能否连助手；用下面这条：
curl -s "https://xiaozhi.cyberai.top/bridge/connector?device_mac=你的MAC"
```

- `bridge disabled` → 云端未开桥接，需在云端库 `server_configs` 设 `bridge_enabled: true` 并重启服务
- `websocket upgrade failed` → 桥接已开、设备校验通过（curl 正常，可开助手）
- `device not found` → 该 MAC 未在**此云**注册

其它示例：

```text
wss://xiaozhi.cyberai.top/bridge/connector
ws://192.168.50.52:8084/bridge/connector
ws://127.0.0.1:8084/bridge/connector
```


#### （2）设备 MAC（必填，当前唯一支持的方式）

在向导或 **连接设置** 的设备标识区域：

1. 选择 **「设备 MAC」**（不要选「连接码 / 验证码」）。
2. 填写与管理台一致的 MAC，格式如：`98:88:e0:14:79:a0`（冒号分隔，大小写不敏感）。

MAC 可在商家云 **设备管理** 列表中查看，见下文第五节截图。

| 项目 | 说明 |
|------|------|
| **当前唯一可用** | **设备 MAC**（在管理台「设备管理」中查看，见第五节截图） |
| **6 位验证码 / 连接码** | **尚未实现，不能使用**；勿选、勿填，否则无法连接 |
| **后续版本计划** | 支持在助手内填写 6 位验证码，由程序向商家云解析 MAC 并写回配置 |

#### （3）OpenClaw（仅勾选 OpenClaw 时显示）

- **连接 OpenClaw 处理任务**：勾选后连真实 Gateway；不勾选则为 **echo 自测**。
- **Gateway Token**：见下文第六节。
- **OpenClaw 地址**：本机常用 `http://127.0.0.1:18789`。

**v0.2.14 说明**：OpenClaw 填本机地址（如 `127.0.0.1:18789`）时，保存后会**自动配置**网关连接方式，**无需**在 `config.yaml` 里手改 `gateway_same_host`。听写默认仅 **inject**（不显示悬浮字幕）。

**从 v0.2.12 升级**：建议删除旧目录下的 `config.yaml` 后重新运行，或在其中设 `first_run: true` 再走一遍向导，并**重新填写**商家云地址。

![首次向导 — 商家云地址、设备 MAC、Gateway Token](./docs/images/04-wizard-server-mac-openclaw.png)

点 **「下一步」**。

### 第 3 步：权限与同意

勾选 **阅读说明** 与 **资源使用** 等同意项；macOS 听写注入需在 **辅助功能** 中允许本程序。点 **「完成配置」**。

![首次向导 — 阅读说明与资源使用同意](./docs/images/10-wizard-consent.png)

---

## 四、配置完成后：主界面与日常使用

### 4.1 运行状态窗口与托盘常驻

- 首次点 **「完成配置」** 后，主窗口可能自动收起；程序在 **菜单栏（macOS）/ 系统托盘** 继续运行，**并未退出**。
- 从托盘点 **「运行状态」** 可再次打开窗口；点 **「退出程序」** 才会真正结束。
- 窗口内显示商家云地址与连接状态（**已连接商家云** 表示成功）。
- **连接设置** / **重连云端** / **退出程序**。
- 系统托盘：运行状态、连接设置、通道设置、重连、退出。

### 4.2 修改配置

托盘或运行状态窗 → **连接设置** → **保存并重连**。  
`config.yaml` 在程序同目录（或系统配置目录）。

![运行状态 — 已连接商家云](./docs/images/06-status-connected.png)

<!-- 可选截图：07-settings-server-url.png、08-tray-menu.png -->

---

## 五、如何获取设备 MAC 地址

### 方式 A：商家云管理台（推荐）

1. 打开商家云 Web 入口并登录（公网云示例：**https://xiaozhi.cyberai.top**），进入 **设备管理**。
2. 打开 **设备管理**，在列表中找到目标设备。
3. 复制该设备的 **MAC 地址**（设备 ID），填入赛搏小小助手向导或连接设置的 **「设备 MAC」** 栏。

![管理后台 — 设备管理中查看设备 MAC](./docs/images/09-admin-device-mac.png)

### 方式 B：机身标贴 / 烧录日志 / 开发配置

从设备包装盒、ESP 烧录日志或 `config.yaml` 等查看，格式须为 `AA:BB:CC:DD:EE:FF`。

### 关于 6 位验证码 / 连接码（当前版本未实现）

赛搏 AI **管理后台**或设备端可能展示 **6 位电脑连接码 / 验证码**（用于日后「不用记 MAC、只填 6 位数字」配对）。  
**v0.2.14 赛搏小小助手尚未实现该功能**，具体说明如下：

| 问题 | 说明 |
|------|------|
| 能在助手里填 6 位验证码吗？ | **不能**。选「连接码」或填写 6 位数字 **无法完成连接**。 |
| 管理台的连接码还有用吗？ | 对 **设备/other 用途** 可能仍有；对 **本版助手** 请 **忽略**，不要按连接码方式配置助手。 |
| 什么时候能用？ | **后续版本**实现后，会在 Release 说明与本指南中更新；届时可填验证码自动解析 MAC。 |
| 现在应该怎么配？ | 只用 **设备 MAC**，从管理台「设备管理」复制（见上图 `09-admin-device-mac.png`）。 |

<!-- 后续版本计划启用的 API（v0.2.14 助手未调用）：
POST /api/public/connector-code-resolve  {"code":"123456"}
POST /api/device/connector-pair-code     {"device_mac":"..."}
-->

---

## 六、如何获取 OpenClaw Gateway Token

> 与商家云 **设备 MAC** **不是同一项**；仅在使用真实 OpenClaw（`gateway_ws`）时需要。

**macOS / Linux：**

```bash
jq -r '.gateway.auth.token' ~/.openclaw/openclaw.json
```

**Windows（PowerShell）：**

```powershell
(Get-Content "$env:USERPROFILE\.openclaw\openclaw.json" | ConvertFrom-Json).gateway.auth.token
```

启动 Gateway（示例，以本机 OpenClaw 为准）：

```bash
openclaw gateway
```

再填 **OpenClaw 地址**（如 `http://127.0.0.1:18789`）与 **Gateway Token**。

---

## 七、命令行启动（可选）

```bash
./SaiboAssistant-macOS-arm64
./SaiboAssistant-macOS-arm64 -config /绝对路径/config.yaml
./SaiboAssistant-macOS-arm64 -config ./config.yaml -headless   # 无界面，须已有 config
```

Windows：`.\SaiboAssistant-Windows-x64.exe`

---

## 八、配置完成后如何使用

1. **OpenClaw 任务**：助手已连云、OpenClaw 通道已开、Gateway 已运行；设备绑定正确后即可派发任务。
2. **听写**：开启听写通道；设备上「开始传输」；注入模式需先点好 PC 输入框。
3. **保持在线**：程序需常驻；退出后设备侧显示 Connector 离线。

---

## 九、配置文件与安全

| 项目 | 说明 |
|------|------|
| 配置位置 | 与 `SaiboAssistant-*` **同目录** 的 `config.yaml` |
| 敏感信息 | 含 Gateway Token 等，勿外传 |
| v0.2.14 单文件 | **不含** 源码；配置由首次运行 **自动生成** |

---

## 十、常见问题

| 现象 | 处理 |
|------|------|
| Mac 无法打开 | 系统设置 → 仍要打开；或 `xattr -cr ./SaiboAssistant-macOS-arm64` |
| 换商家云后界面仍显示旧地址 | v0.2.14 已修复；旧版请用「连接设置 → 保存并重连」，或查看 `config.yaml` 中 `server_url` |
| 配置完成后窗口消失 | **正常**：程序在托盘常驻，从托盘打开「运行状态」 |
| 仍用旧版连不上或崩溃 | 请升级到 **v0.2.14** 并重新走首次向导 |
| 无法连接商家云 | 检查 `server_url`、端口、`bridge_enabled`；v0.2.14 须**手动填写**完整 `ws://.../bridge/connector` |
| 误用了 6 位验证码 / 连接码 | **本版未实现**；在向导或连接设置中改选 **「设备 MAC」**，填管理台中的 MAC 后保存并重连 |
| 用了连接码仍连不上 | 属预期行为；**请勿使用验证码**，仅支持 MAC |
| `device not found` | MAC 与当前商家云不一致，或设备未在该云注册 |
| OpenClaw 失败 | Gateway 是否运行、Token 是否与 `openclaw.json` 一致 |
| `missing scope: operator.write` | 请升级到 **v0.2.14+**；本机 OpenClaw 会自动处理；改配置后点 **重连云端** 或完全退出再开 |
| macOS 听写不注入 | 辅助功能中允许本程序 |

---

## 相关链接

- **v0.2.14 Release**：https://github.com/nakakak/SaiboAssistant/releases/tag/v0.2.14

如有商家云地址、管理台账号等问题，请联系服务提供方或运维。
